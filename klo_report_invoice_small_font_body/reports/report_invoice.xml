<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <template id="klo_invoice_report_small_font_body" inherit_id="account.report_invoice_document">

    <xpath expr="//t[@t-set='o'][@t-value='o.with_context(lang=lang)']" position="before">
        <t t-set="font_size" t-value="request.env['ir.config_parameter'].sudo().get_param('klo.invoice_report_small_font_body', '8')"/>
        <style>
            /* Forzar tamaño dinámico en todo el documento */
            body, body * {
                font-size: <t t-esc="font_size"/>px !important;
            }

            /* Ajustar padding para compensar el tamaño reducido */
            .table th, .table td {
                padding: 1px;
            }

            /* Títulos proporcionales */
            h1 { font-size: <t t-esc="int(font_size) * 1.5"/>px !important; }
            h2 { font-size: <t t-esc="int(font_size) * 1.25"/>px !important; }
            h3 { font-size: <t t-esc="int(font_size) * 1.125"/>px !important; }

            /* Mejorar legibilidad con fuente monoespaciada */
            body {
                font-family: 'Courier New', monospace;
                line-height: 1.2;
            }

            /* Tamaño específico para el título del documento */
            .document-title, .document-title * {
                font-size: <t t-esc="int(font_size) * 1.5"/>px !important;
            }
        </style>
    </xpath>

    <!-- Añade atributo inline al campo que imprime el nombre/número de factura para que sea más grande -->
    <xpath expr="//t[@t-set='layout_document_title']" position="replace">
        <t t-set="layout_document_title">
            <div class="document-title">
                <span t-if="not proforma"/>
                <span t-else="">PROFORMA</span>
                <span t-if="o.move_type == 'out_invoice' and o.state == 'posted'">Invoice</span>
                <span t-elif="o.move_type == 'out_invoice' and o.state == 'draft'">Draft Invoice</span>
                <span t-elif="o.move_type == 'out_invoice' and o.state == 'cancel'">Cancelled Invoice</span>
                <span t-elif="o.move_type == 'out_refund' and o.state == 'posted'">Credit Note</span>
                <span t-elif="o.move_type == 'out_refund' and o.state == 'draft'">Draft Credit Note</span>
                <span t-elif="o.move_type == 'out_refund' and o.state == 'cancel'">Cancelled Credit Note</span>
                <span t-elif="o.move_type == 'in_refund'">Vendor Credit Note</span>
                <span t-elif="o.move_type == 'in_invoice'">Vendor Bill</span>
                <span t-if="o.name and o.name != '/'" t-field="o.name">INV/2023/0001</span>
            </div>
        </t>
    </xpath>

  </template>
</odoo>
